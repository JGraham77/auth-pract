import React from "react";

const Home = (props: HomeProps) => {
    return (
        <div>
            <h1 className="text-center">Home</h1>
        </div>
    );
};

interface HomeProps {}

export default Home;
